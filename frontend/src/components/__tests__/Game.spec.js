import { describe, it, expect } from 'vitest'

import { mount } from '@vue/test-utils'

import Game from '../Game.vue'

// describe('Game', () => {
//   it('renders properly', () => {
//     const wrapper = mount(Game, { props: { msg: 'Hello Vitest' } })
//     expect(wrapper.text()).toContain('Hello Vitest')
//   })
// })
